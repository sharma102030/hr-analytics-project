export default function Footer() {
  return (
    <footer className="footer">
      <p>
        Data: a fictional, publicly available IBM HR analytics dataset (1,470 employee records).
        Risk flags are rule-based (overtime, promotion gap, satisfaction, work-life balance),
        not machine-learning predictions — a deliberate first step before investing in a model.
      </p>
    </footer>
  );
}
