// Mirrors the CSS variables in index.css. Kept as plain JS constants because
// chart libraries (Recharts) need real color values, not CSS custom properties,
// for some of their internal SVG calculations.
export const COLORS = {
  teal: '#2f6f62',
  rust: '#a8462b',
  amber: '#b8862e',
  border: '#d7dbd8',
  inkMuted: '#5b6660'
};
